# Local instructions

Your own instructions for AI agents working in this vault. Nothing here is
managed by the workhub app: the app seeds this file once and never touches it
again, so anything you write survives every app update.

Put personal or machine-specific guidance here rather than editing `CLAUDE.md`,
which the app keeps in sync with its bundled template. Where this file and
`CLAUDE.md` disagree, this file wins.

Delete the placeholder below and write whatever you need — or leave the file
empty, which simply means "no local instructions".

<!-- Example:
- Reply in Japanese, but keep code comments in English.
- The main development machine is Windows; prefer PowerShell examples.
-->
