[
  {
    "id": "profile-to-memory",
    "since": "0.139.0",
    "severity": "breaking",
    "title": "Your profile has not moved into the vault's memory yet",
    "summary": "profile/ is now memory/identity/. Until it moves, agents are not being told who you are.",
    "body": "`profile/` became `memory/identity/` in 0.139.0, and the decision log became one typed note per call under `memory/notes/`. The two folders were the same thing described twice — what a session is told about you before it starts.\n\n**This vault has not made the move, and nothing will say so again.** The hook that injects your context looks for `memory/identity/decision-policy.md`, finds nothing, and exits without an error. Every session since has been starting without knowing who you are or how you decide.\n\nTwo more things make it worse than it looks:\n\n- The vault template adds files it finds missing, without asking. It will seed a **blank** `memory/identity/about-me.md` beside the one you actually wrote, and from then on the blank one is the one being read.\n- Leftover files are only listed when they still match the template byte for byte, so anything you edited yourself never appears in that list.\n\nThe migration is one skill. It shows you what would move before touching anything, refuses a vault with uncommitted work, never deletes and never overwrites, and tells you which links the move breaks.",
    "when": {
      "path_exists": ["profile/decision-policy.md"],
      "unless_template": [
        "bbddfe0764d89caf5cf6ae171a40cb1ccf922d83c9b3feecd14b1789bc15a5ff",
        "e5403858037e45a2babe37f317788103ad44b83c33f46ee3c485f3db1d6f3298"
      ]
    },
    "requires": {
      "plugin": { "workhub": "0.40.1" }
    },
    "action": {
      "title": "Move profile/ to memory/identity/",
      "project": "workhub",
      "body": "This vault is still on the pre-0.139.0 layout: the owner's context lives in `profile/`, while the agent hooks read `memory/identity/`. The hooks fail silently on the missing path, so every session is starting without the owner's profile or decision policy.\n\nRun the `vault-upgrade` skill from the workhub plugin:\n\n1. `status` — confirm migration `001` reports as NEEDED.\n2. `plan 001` — show the owner exactly what would move, and wait for their approval. It touches nothing.\n3. `apply 001` — only after they agree. It refuses a vault with uncommitted changes; commit or discard first rather than working around it.\n\nWhat the migration does **not** do is as important as what it does. It never deletes and never overwrites: `profile/decision-log.md` is left in place after its contents become notes under `memory/notes/`, and a file already occupying a destination is moved aside rather than replaced. Removing anything is the owner's decision, and only after the new notes have been read and committed.\n\nAfterwards:\n\n- Have the owner review the diff and commit it. An uncommitted migration is one crash away from being lost.\n- Report the links the migration lists as broken. Do not rewrite them — most are records of what was true at the time, not links to repair.\n- Say plainly what moved, what was left alone, and what was not deleted."
    }
  },
  {
    "id": "ai-memory-to-state",
    "since": "0.144.0",
    "severity": "breaking",
    "title": "_ai/memory/ is now _ai/state/ — this vault has not migrated",
    "summary": "_ai/memory/ was renamed to _ai/state/ so it stops being confused with the memory/ knowledge layer. As of T-0392, nothing falls back to the old name any more: memory, session task markers, and the tidy pending list are stranded in _ai/memory/ until this vault runs the migration.",
    "body": "`_ai/memory/` held session markers, the tidy pending list, edit snapshots, soft-delete trash and the memory engine's database — app and agent working data, never knowledge. Its name collided with the unrelated `memory/` knowledge layer at the vault root, so T-0390 renamed it to `_ai/state/`.\n\n**This vault has not migrated, and the app no longer works around it.** Every reader and writer now resolves the folder as `_ai/state/` only. Until you run the migration: long-term memory stops recording new turns, a session's task marker is not found (so brief/inject skip it), and the tidy pending list looks empty even though `_ai/memory/tidy-pending.json` still holds real entries. None of this data is lost — it is exactly where T-0390 left it — but nothing reads or writes it until the folder moves.\n\nThe migration is one skill. It shows you what would move before touching anything, refuses a vault with uncommitted work, never deletes and never overwrites — a real collision between the same file in both folders stops the run and asks you to resolve it by hand.",
    "when": {
      "path_exists": ["_ai/memory"]
    },
    "requires": {
      "plugin": { "workhub": "0.43.0" }
    },
    "action": {
      "title": "Move _ai/memory/ to _ai/state/",
      "project": "workhub",
      "body": "This vault still has files under `_ai/memory/`, renamed to `_ai/state/` in T-0390. Since T-0392, nothing falls back to the old name any more, so memory, session task markers, and the tidy pending list are stranded there until this vault migrates.\n\nRun the `vault-upgrade` skill from the workhub plugin:\n\n1. `status` — confirm migration `002` reports as NEEDED.\n2. `plan 002` — show the owner exactly what would move, and wait for their approval. It touches nothing.\n3. `apply 002` — only after they agree. It refuses a vault with uncommitted changes; commit or discard first rather than working around it.\n\nIt never deletes and never overwrites: a file already at the destination is left alone when identical, and a real collision (different content on both sides) stops the run rather than guessing which one is the owner's. `_ai/memory/memory.db` and its `-wal`/`-shm` siblings are gitignored either way.\n\nAfterwards, have the owner review the diff and commit it, then remove the now-empty `_ai/memory/` folder by hand once they are happy."
    }
  }
]
