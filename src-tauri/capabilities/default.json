{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window, the ink overlay, the ink preview window, the task editor window, the quick-capture window, the voice-indicator window, the clips popup, and the Docs viewer windows (one per pop-out, labelled docs-viewer-<n>)",
  "windows": ["main", "ink-overlay", "ink-preview", "task-editor", "quick-capture", "voice-indicator", "clips-popup", "docs-viewer-*"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:default",
    "clipboard-manager:allow-write-text",
    "clipboard-manager:allow-write-image",
    "clipboard-manager:allow-read-text",
    "opener:default",
    "notification:default"
  ]
}
