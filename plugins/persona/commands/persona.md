---
description: 応答口調のキャラクターを切り替える
argument-hint: "[<キャラクター>] [<レベル>] | 一時 | off"
---

ペルソナを $ARGUMENTS に切り替える。引数なしならキャラクター一覧と現在の状態を表示。
切替の処理はフックが行うため、`characters/` や `core/` のファイルを読まないこと。
技術用語・否定・限定・例外・数値・単位は維持。コードは無変更。
